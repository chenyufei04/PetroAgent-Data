# 项目拉取、安装与更新指南

本文档说明如何在 Windows 或 macOS 上拉取石油工程术语与数据质控系统、安装前后端依赖、启动项目，以及后续同步 GitHub 上的最新代码。

项目仓库：<https://github.com/chenyufei04/PetroAgent-Data.git>

## 快速拉取代码

### 第一次下载项目

本机还没有该项目时，执行：

```text
git clone https://github.com/chenyufei04/PetroAgent-Data.git
cd PetroAgent-Data
```

### 更新已经下载的项目

本机已经存在该项目时，进入项目目录后执行：

Windows PowerShell：

```powershell
cd F:\Projects\PetroAgent-Data
git status
git pull origin main
```

macOS Terminal：

```bash
cd ~/Projects/PetroAgent-Data
git status
git pull origin main
```

其中：

- `git clone`：第一次把整个远程仓库下载到本机，只需执行一次。
- `git pull origin main`：将远程 `main` 分支的最新提交同步到当前本地项目。
- `git status`：拉取前检查本地是否存在未提交修改，防止更新时产生冲突。

如果本地有需要保留的未提交修改，请不要直接覆盖，先按照本文“以后拉取最新代码”章节进行提交或暂存。

## 一、环境要求

安装前请准备：

- Git
- Node.js 18 或更高版本
- npm
- Python 3.10

检查版本：

```text
git --version
node --version
npm --version
python --version
```

macOS 中也可以使用以下命令检查 Python 3.10：

```bash
python3.10 --version
```

## 二、第一次拉取项目

### Windows

在 PowerShell 中进入准备存放项目的目录：

```powershell
cd F:\Projects
git clone https://github.com/chenyufei04/PetroAgent-Data.git
cd PetroAgent-Data
```

### macOS

在 Terminal 中执行：

```bash
mkdir -p ~/Projects
cd ~/Projects
git clone https://github.com/chenyufei04/PetroAgent-Data.git
cd PetroAgent-Data
```

如果仓库为私有仓库，GitHub 会要求身份验证。通过 HTTPS 拉取时，应使用浏览器授权、Personal Access Token 或 GitHub CLI，不能使用 GitHub 账户密码直接验证。

## 三、关联 GitHub 账户并确认推送权限

Git 的“提交身份”和 GitHub 的“推送认证”是两件事，二者都要配置：

- `user.name` 和 `user.email` 决定提交记录显示的作者。
- HTTPS 登录、GitHub CLI 或 SSH 密钥决定当前电脑有没有权限推送到 GitHub。

### 1. 配置提交姓名和邮箱

先查看当前配置：

```text
git config --global user.name
git config --global user.email
```

没有配置时，设置 GitHub 用户名和已在 GitHub 验证的邮箱：

```text
git config --global user.name "chenyufei04"
git config --global user.email "你的GitHub已验证邮箱"
```

`--global` 会应用于当前电脑上的所有仓库。如果只想为本项目配置，请进入项目目录后去掉 `--global`：

```text
git config user.name "chenyufei04"
git config user.email "你的GitHub已验证邮箱"
```

配置后检查实际生效值及其来源：

```text
git config --show-origin --get user.name
git config --show-origin --get user.email
```

如果 GitHub 开启了“阻止暴露私人邮箱”，建议打开 GitHub 的 **Settings → Emails**，复制页面提供的 `noreply` 邮箱，然后把它设置为 Git 提交邮箱。例如：

```text
git config --global user.email "GitHub页面提供的ID+用户名@users.noreply.github.com"
```

不要照抄示例文字，应使用 GitHub 设置页面显示的完整地址。GitHub 根据提交中的邮箱将提交关联到对应账户。

### 2. 检查远程仓库地址

```text
git remote -v
```

正常情况下应看到：

```text
origin  https://github.com/chenyufei04/PetroAgent-Data.git (fetch)
origin  https://github.com/chenyufei04/PetroAgent-Data.git (push)
```

如果没有 `origin`，执行：

```text
git remote add origin https://github.com/chenyufei04/PetroAgent-Data.git
```

如果 `origin` 地址错误，执行：

```text
git remote set-url origin https://github.com/chenyufei04/PetroAgent-Data.git
```

### 3. 推荐方式：使用 HTTPS 登录 GitHub

安装最新版 Git for Windows 后通常会包含 Git Credential Manager。首次执行 `git push` 时，按提示在浏览器中登录并授权 GitHub，凭据会保存在 Windows 凭据管理器中。

也可以安装 [GitHub CLI](https://cli.github.com/) 后执行：

```text
gh auth login
```

按提示选择：

```text
GitHub.com
HTTPS
Login with a web browser
```

登录后检查状态：

```text
gh auth status
```

GitHub 不再接受账户密码作为 Git HTTPS 推送密码。若不使用浏览器登录，可以创建 Personal Access Token，并在提示输入密码时粘贴令牌。

### 4. 可选方式：使用 SSH 密钥

创建 SSH 密钥时，将邮箱替换为自己的 GitHub 已验证邮箱：

```text
ssh-keygen -t ed25519 -C "你的GitHub已验证邮箱"
```

将生成的 `.pub` 公钥内容添加到 GitHub 的 **Settings → SSH and GPG keys**，然后测试：

```text
ssh -T git@github.com
```

测试成功后把项目远程地址改为 SSH：

```text
git remote set-url origin git@github.com:chenyufei04/PetroAgent-Data.git
```

HTTPS 和 SSH 选择一种即可，不需要同时使用。

### 5. 第一次推送

确认提交身份、远程地址和认证均正确后执行：

```text
git branch --show-current
git push -u origin main
```

以后在 `main` 分支通常只需执行：

```text
git push
```

### 6. 常见推送失败处理

#### `GH007: Your push would publish a private email address`

这表示某个待推送提交使用了被 GitHub 保护的私人邮箱。先将 `user.email` 改为 GitHub 提供的 `noreply` 邮箱。如果问题只存在于最新提交，可重新写入最新提交的作者信息：

```text
git commit --amend --reset-author --no-edit
git push -u origin main
```

该操作会改变最新提交的哈希。如果多个历史提交都包含受保护邮箱，不要直接强制推送，应先备份分支，再统一重写相应提交的作者邮箱。

#### `Permission denied` 或 HTTP 403

通常表示登录了错误账户、当前账户没有仓库写权限，或者缓存凭据已经失效。确认登录账户为 `chenyufei04` 或已被授予该仓库写权限的账户。Windows 可以在“凭据管理器”中删除旧的 GitHub 凭据后重新推送并登录；GitHub CLI 可以执行：

```text
gh auth logout
gh auth login
```

#### `Authentication failed`

不要输入 GitHub 账户密码。请使用浏览器授权、GitHub CLI、Git Credential Manager、Personal Access Token 或 SSH 密钥。

#### `remote origin already exists`

说明远程仓库已经配置，无需再次执行 `git remote add origin`。使用 `git remote -v` 检查，必要时通过 `git remote set-url origin ...` 修改。

GitHub 官方说明可参阅：[设置提交邮箱](https://docs.github.com/en/account-and-profile/how-tos/email-preferences/setting-your-commit-email-address)、[缓存 HTTPS 凭据](https://docs.github.com/en/get-started/git-basics/caching-your-github-credentials-in-git)和[通过 SSH 连接 GitHub](https://docs.github.com/en/authentication/connecting-to-github-with-ssh)。

## 四、安装前端依赖

Windows 和 macOS 均在项目根目录执行：

```text
npm install
```

该命令会根据 `package.json` 和 `package-lock.json` 安装 Vue、Vite、TypeScript 等前端依赖。`node_modules/` 不需要提交到 Git。

## 五、安装 Python 后端依赖

项目使用 Python 3.10，并通过项目根目录下的 `requirements.txt` 管理后端依赖。

### Windows

创建虚拟环境：

```powershell
py -3.10 -m venv .venv
```

激活虚拟环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

如果 PowerShell 阻止激活脚本运行，可只为当前终端临时允许脚本执行：

```powershell
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\.venv\Scripts\Activate.ps1
```

使用清华大学 PyPI 镜像安装依赖：

```powershell
python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### macOS

如果尚未安装 Python 3.10，可通过 Homebrew 安装：

```bash
brew install python@3.10
```

创建并激活虚拟环境：

```bash
python3.10 -m venv .venv
source .venv/bin/activate
```

使用清华大学 PyPI 镜像安装依赖：

```bash
python -m pip install --upgrade pip -i https://pypi.tuna.tsinghua.edu.cn/simple
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> `.venv/` 是当前电脑上的本地虚拟环境，不会随 Git 仓库上传或下载。更换电脑或操作系统后需要重新创建。

## 六、启动项目

项目需要分别启动 FastAPI 后端和 Vue 前端，建议打开两个终端，并确保两个终端当前目录都是项目根目录。

### 启动后端

Windows PowerShell：

```powershell
.\.venv\Scripts\Activate.ps1
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

macOS Terminal：

```bash
source .venv/bin/activate
python -m uvicorn backend.main:app --host 127.0.0.1 --port 8000
```

后端接口文档：<http://127.0.0.1:8000/docs>

### 启动前端

Windows 和 macOS 均执行：

```text
npm run dev
```

浏览器访问：<http://127.0.0.1:5173>

## 七、以后拉取最新代码

已经克隆过项目时，不需要再次执行 `git clone`。进入现有项目目录后，先检查本地状态：

```text
git status
git branch --show-current
```

确认当前分支为 `main` 且没有需要保留的未提交修改后，执行：

```text
git pull origin main
```

如果拉取后的 `package-lock.json` 或 `requirements.txt` 有变化，重新安装依赖：

```text
npm install
```

激活 Python 虚拟环境后执行：

```text
python -m pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 本地存在未提交修改

建议先提交本地修改，再拉取远程代码：

```text
git add .
git commit -m "保存本地修改"
git pull --rebase origin main
```

如果暂时不准备提交，可以临时保存修改：

```text
git stash push -m "拉取前临时保存"
git pull origin main
git stash pop
```

执行 `git stash pop` 后如果出现冲突，需要人工确认并合并冲突内容。

## 八、构建验证

安装完成或拉取重要更新后，可以执行生产构建验证：

```text
npm run build
```

构建成功后，生产文件会生成在 `dist/` 目录。

## 九、常见问题

### `ECONNREFUSED 127.0.0.1:8000`

表示 Vue 前端无法连接 FastAPI 后端。请确认后端终端仍在运行，并访问 <http://127.0.0.1:8000/docs> 检查接口是否可用。

### 端口被占用

如果 `8000` 或 `5173` 已被其他程序使用，请先关闭之前启动的项目进程。修改后端端口时，还需要同步调整 `vite.config.ts` 中的代理地址。

### 找不到 `python3.10`

确认 Python 3.10 已安装并加入系统路径。Windows 推荐使用 `py -3.10`，macOS 推荐通过 Homebrew 安装 `python@3.10`。

### 清华镜像安装失败

先检查网络和镜像地址是否能够访问。必要时可以暂时改用官方 PyPI：

```text
python -m pip install -r requirements.txt
```

### 拉取后没有上传文件

`uploads/`、`.venv/`、`node_modules/` 等本地目录通常被 `.gitignore` 忽略，不会随代码仓库同步。上传文件如需迁移，应在符合数据安全和保密要求的前提下单独备份。

## 十、完整的首次安装顺序

```text
安装 Git、Node.js、npm、Python 3.10
                  ↓
             git clone
                  ↓
      配置提交身份并登录 GitHub
                  ↓
             npm install
                  ↓
        创建并激活 .venv
                  ↓
使用 requirements.txt 安装 Python 依赖
                  ↓
      分别启动后端和前端
                  ↓
       访问 127.0.0.1:5173
```
